package rsa;

import org.junit.Test;

public class signtest {
    @Test
    public void sign() throws Exception {
//        KeyPair keyPair = RSAUtils.generateRSAKeyPair(1024);
//        PrivateKey privateKey = RSAUtils.getPrivateKey(keyPair.getPrivate().getEncoded());
//        PublicKey publicKey = RSAUtils.getPublicKey(keyPair.getPublic().getEncoded());
//        new FileOutputStream("D:\\工程项目\\armardillo\\src\\main\\resources\\static\\rsa\\en_private.txt").write(Base64.getEncoder().encode(privateKey.getEncoded()));
//        new FileOutputStream("D:\\工程项目\\armardillo\\src\\main\\resources\\static\\rsa\\en_public.txt").write(Base64.getEncoder().encode(publicKey.getEncoded()));
//        keyPair = RSAUtils.generateRSAKeyPair(1024);
//        privateKey = RSAUtils.getPrivateKey(keyPair.getPrivate().getEncoded());
//        publicKey = RSAUtils.getPublicKey(keyPair.getPublic().getEncoded());
//        new FileOutputStream("D:\\工程项目\\armardillo\\src\\main\\resources\\static\\rsa\\sign_private.txt").write(Base64.getEncoder().encode(privateKey.getEncoded()));
//        new FileOutputStream("D:\\工程项目\\armardillo\\src\\main\\resources\\static\\rsa\\sign_public.txt").write(Base64.getEncoder().encode(publicKey.getEncoded()));
//
//        String sign = RSASignature.sign("1234", Base64.getEncoder().encodeToString(privateKey.getEncoded()));
//        System.out.println(sign);
        //System.out.println(RSASignature.doCheck("1234", sign, Base64.getEncoder().encodeToString(publicKey.getEncoded())));
    }
}
